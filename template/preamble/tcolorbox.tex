\usetikzlibrary{arrows,calc,shadows.blur}
\tikzset{
    symbol/.style={
        draw=none,
        every to/.append style={
            edge node={node [sloped, allow upside down, auto=false]{$#1$}}}
    }
}
\tikzstyle{c} = [circle,fill=tnFg,scale=0.5]
\tikzstyle{b} = [draw, thick, tnFg, -]
\tikzset{
    vertex/.style={
        circle, draw, minimum size=6mm, inner sep=0pt,
        draw=tnBlue, text=tnFg
    }
}

\renewcommand\qedsymbol{$\Laughey$}

% ----------------------------------------------------------------
%  ALGORITHM COMMENT STYLE
% ----------------------------------------------------------------
\newcommand\mycommfont[1]{\footnotesize\ttfamily\textcolor{tnComment}{#1}}
\SetCommentSty{mycommfont}

\newcommand{\incfig}[1]{%
    \def\svgwidth{\columnwidth}
    \import{./figures/}{#1.pdf_tex}
}



% ----------------------------------------------------------------
%  TCOLORBOX SETUPS  (Tokyo Night Dark)
% ----------------------------------------------------------------
\tcbuselibrary{theorems,skins,hooks}
\setlength{\parindent}{1cm}

% ============================================================
%  CUSTOM NOTE-STYLE BOXES — Tokyo Night Dark
%  Inspired by the note box style in your template
% ============================================================

\newtcolorbox{learning}[1][]{%
	enhanced jigsaw,
	colback=tnBgHL, coltext=tnFg,
	colframe=tnYellow,
	size=small, boxrule=1pt,
	title=\textbf{\textcolor{tnYellow}{FOR LEARNING PURPOSE}},
	halign title=flush center,
	coltitle=tnYellow,
	breakable,
	drop shadow=tnBgDark!80!tnComment,
	attach boxed title to top left={xshift=1cm,yshift=-\tcboxedtitleheight/2,
		yshifttext=-\tcboxedtitleheight/2},
	minipage boxed title=6cm,
	boxed title style={%
		colback=tnBgDark, coltext=tnYellow,
		size=fbox, boxrule=1pt, boxsep=2pt,
		underlay={%
			\coordinate (dotA) at ($(interior.west) + (-0.5pt,0)$);
			\coordinate (dotB) at ($(interior.east) + (0.5pt,0)$);
			\begin{scope}
				\clip (interior.north west) rectangle ([xshift=3ex]interior.east);
				\filldraw [tnBgDark, blur shadow={shadow opacity=60, shadow yshift=-.75ex},
				rounded corners=2pt] (interior.north west) rectangle (interior.south east);
			\end{scope}
			\begin{scope}[tnYellow]
				\fill (dotA) circle (2pt);
				\fill (dotB) circle (2pt);
			\end{scope}
		},
	},
	#1,
}

% ----------------------------------------
% LAYER 1 — Explain Like I'm 5 (Yellow - friendly/approachable)
% ----------------------------------------
\newtcolorbox{layerELI5}[1][]{%
	enhanced jigsaw,
	colback=tnBgHL, coltext=tnFg,
	colframe=tnYellow,
	size=small, boxrule=1pt,
	title=\textbf{\textcolor{tnYellow}{Explain Like I'm 5}},
	halign title=flush center,
	coltitle=tnYellow,
	breakable,
	drop shadow=tnBgDark!80!tnComment,
	attach boxed title to top left={xshift=1cm,yshift=-\tcboxedtitleheight/2,
		yshifttext=-\tcboxedtitleheight/2},
	minipage boxed title=6cm,
	boxed title style={%
		colback=tnBgDark, coltext=tnYellow,
		size=fbox, boxrule=1pt, boxsep=2pt,
		underlay={%
			\coordinate (dotA) at ($(interior.west) + (-0.5pt,0)$);
			\coordinate (dotB) at ($(interior.east) + (0.5pt,0)$);
			\begin{scope}
				\clip (interior.north west) rectangle ([xshift=3ex]interior.east);
				\filldraw [tnBgDark, blur shadow={shadow opacity=60, shadow yshift=-.75ex},
				rounded corners=2pt] (interior.north west) rectangle (interior.south east);
			\end{scope}
			\begin{scope}[tnYellow]
				\fill (dotA) circle (2pt);
				\fill (dotB) circle (2pt);
			\end{scope}
		},
	},
	#1,
}

% ----------------------------------------
% LAYER 2 — The Real Explanation (Blue - standard/technical)
% ----------------------------------------
\newtcolorbox{layerReal}[1][]{%
	enhanced jigsaw,
	colback=tnBgHL, coltext=tnFg,
	colframe=tnBlue,
	size=small, boxrule=1pt,
	title=\textbf{\textcolor{tnBlue}{The Real Explanation}},
	halign title=flush center,
	coltitle=tnBlue,
	breakable,
	drop shadow=tnBgDark!80!tnComment,
	attach boxed title to top left={xshift=1cm,yshift=-\tcboxedtitleheight/2,
		yshifttext=-\tcboxedtitleheight/2},
	minipage boxed title=6cm,
	boxed title style={%
		colback=tnBgDark, coltext=tnBlue,
		size=fbox, boxrule=1pt, boxsep=2pt,
		underlay={%
			\coordinate (dotA) at ($(interior.west) + (-0.5pt,0)$);
			\coordinate (dotB) at ($(interior.east) + (0.5pt,0)$);
			\begin{scope}
				\clip (interior.north west) rectangle ([xshift=3ex]interior.east);
				\filldraw [tnBgDark, blur shadow={shadow opacity=60, shadow yshift=-.75ex},
				rounded corners=2pt] (interior.north west) rectangle (interior.south east);
			\end{scope}
			\begin{scope}[tnBlue]
				\fill (dotA) circle (2pt);
				\fill (dotB) circle (2pt);
			\end{scope}
		},
	},
	#1,
}

% ----------------------------------------
% LAYER 3 — Key Takeaways (Green - summary/actionable)
% ----------------------------------------
\newtcolorbox{layerTakeaways}[1][]{%
	enhanced jigsaw,
	colback=tnBgHL, coltext=tnFg,
	colframe=tnGreen,
	size=small, boxrule=1pt,
	title=\textbf{\textcolor{tnGreen}{Key Takeaways}},
	halign title=flush center,
	coltitle=tnGreen,
	breakable,
	drop shadow=tnBgDark!80!tnComment,
	attach boxed title to top left={xshift=1cm,yshift=-\tcboxedtitleheight/2,
		yshifttext=-\tcboxedtitleheight/2},
	minipage boxed title=4cm,
	boxed title style={%
		colback=tnBgDark, coltext=tnGreen,
		size=fbox, boxrule=1pt, boxsep=2pt,
		underlay={%
			\coordinate (dotA) at ($(interior.west) + (-0.5pt,0)$);
			\coordinate (dotB) at ($(interior.east) + (0.5pt,0)$);
			\begin{scope}
				\clip (interior.north west) rectangle ([xshift=3ex]interior.east);
				\filldraw [tnBgDark, blur shadow={shadow opacity=60, shadow yshift=-.75ex},
				rounded corners=2pt] (interior.north west) rectangle (interior.south east);
			\end{scope}
			\begin{scope}[tnGreen]
				\fill (dotA) circle (2pt);
				\fill (dotB) circle (2pt);
			\end{scope}
		},
	},
	#1,
}

% ----------------------------------------
% MISCONCEPTION ALERT (Red/Magenta - warning/danger)
% ----------------------------------------
\newtcolorbox{misconception}[1][]{%
	enhanced jigsaw,
	colback=tnBgHL, coltext=tnFg,
	colframe=tnMagenta,
	size=small, boxrule=1pt,
	title=\textbf{\textcolor{tnMagenta}{MISCONCEPTION ALERT}},
	halign title=flush center,
	coltitle=tnMagenta,
	breakable,
	drop shadow=tnBgDark!80!tnComment,
	attach boxed title to top left={xshift=1cm,yshift=-\tcboxedtitleheight/2,
		yshifttext=-\tcboxedtitleheight/2},
	minipage boxed title=6cm,
	boxed title style={%
		colback=tnBgDark, coltext=tnMagenta,
		size=fbox, boxrule=1pt, boxsep=2pt,
		underlay={%
			\coordinate (dotA) at ($(interior.west) + (-0.5pt,0)$);
			\coordinate (dotB) at ($(interior.east) + (0.5pt,0)$);
			\begin{scope}
				\clip (interior.north west) rectangle ([xshift=3ex]interior.east);
				\filldraw [tnBgDark, blur shadow={shadow opacity=60, shadow yshift=-.75ex},
				rounded corners=2pt] (interior.north west) rectangle (interior.south east);
			\end{scope}
			\begin{scope}[tnMagenta]
				\fill (dotA) circle (2pt);
				\fill (dotB) circle (2pt);
			\end{scope}
		},
	},
	#1,
}

% ----------------------------------------
% OPTIONAL — Further Exploration (Purple - curiosity/extra)
% ----------------------------------------
\newtcolorbox{optionalExplore}[1][]{%
	enhanced jigsaw,
	colback=tnBgHL, coltext=tnFg,
	colframe=tnPurple,
	size=small, boxrule=1pt,
	title=\textbf{\textcolor{tnPurple}{Further Exploration}},
	halign title=flush center,
	coltitle=tnPurple,
	breakable,
	drop shadow=tnBgDark!80!tnComment,
	attach boxed title to top left={xshift=1cm,yshift=-\tcboxedtitleheight/2,
		yshifttext=-\tcboxedtitleheight/2},
	minipage boxed title=1.5cm,
	boxed title style={%
		colback=tnBgDark, coltext=tnPurple,
		size=fbox, boxrule=1pt, boxsep=2pt,
		underlay={%
			\coordinate (dotA) at ($(interior.west) + (-0.5pt,0)$);
			\coordinate (dotB) at ($(interior.east) + (0.5pt,0)$);
			\begin{scope}
				\clip (interior.north west) rectangle ([xshift=3ex]interior.east);
				\filldraw [tnBgDark, blur shadow={shadow opacity=60, shadow yshift=-.75ex},
				rounded corners=2pt] (interior.north west) rectangle (interior.south east);
			\end{scope}
			\begin{scope}[tnPurple]
				\fill (dotA) circle (2pt);
				\fill (dotB) circle (2pt);
			\end{scope}
		},
	},
	#1,
}

% ----------------------------------------
% Core Idea (Cyan - central/fundamental)
% ----------------------------------------
\newtcolorbox{coreIdea}[1][]{%
	enhanced jigsaw,
	colback=tnBgHL, coltext=tnFg,
	colframe=tnCyan,
	size=small, boxrule=1pt,
	title=\textbf{\textcolor{tnCyan}{Core Idea}},
	halign title=flush center,
	coltitle=tnCyan,
	breakable,
	drop shadow=tnBgDark!80!tnComment,
	attach boxed title to top left={xshift=1cm,yshift=-\tcboxedtitleheight/2,
		yshifttext=-\tcboxedtitleheight/2},
	minipage boxed title=1.5cm,
	boxed title style={%
		colback=tnBgDark, coltext=tnCyan,
		size=fbox, boxrule=1pt, boxsep=2pt,
		underlay={%
			\coordinate (dotA) at ($(interior.west) + (-0.5pt,0)$);
			\coordinate (dotB) at ($(interior.east) + (0.5pt,0)$);
			\begin{scope}
				\clip (interior.north west) rectangle ([xshift=3ex]interior.east);
				\filldraw [tnBgDark, blur shadow={shadow opacity=60, shadow yshift=-.75ex},
				rounded corners=2pt] (interior.north west) rectangle (interior.south east);
			\end{scope}
			\begin{scope}[tnCyan]
				\fill (dotA) circle (2pt);
				\fill (dotB) circle (2pt);
			\end{scope}
		},
	},
	#1,
}

% ----------------------------------------
% Key Arguments / Findings (Blue - analytical)
% ----------------------------------------
\newtcolorbox{keyArgs}[1][]{%
	enhanced jigsaw,
	colback=tnBgHL, coltext=tnFg,
	colframe=tnBlue,
	size=small, boxrule=1pt,
	title=\textbf{\textcolor{tnBlue}{Key Arguments / Findings}},
	halign title=flush center,
	coltitle=tnBlue,
	breakable,
	drop shadow=tnBgDark!80!tnComment,
	attach boxed title to top left={xshift=1cm,yshift=-\tcboxedtitleheight/2,
		yshifttext=-\tcboxedtitleheight/2},
	minipage boxed title=1.5cm,
	boxed title style={%
		colback=tnBgDark, coltext=tnBlue,
		size=fbox, boxrule=1pt, boxsep=2pt,
		underlay={%
			\coordinate (dotA) at ($(interior.west) + (-0.5pt,0)$);
			\coordinate (dotB) at ($(interior.east) + (0.5pt,0)$);
			\begin{scope}
				\clip (interior.north west) rectangle ([xshift=3ex]interior.east);
				\filldraw [tnBgDark, blur shadow={shadow opacity=60, shadow yshift=-.75ex},
				rounded corners=2pt] (interior.north west) rectangle (interior.south east);
			\end{scope}
			\begin{scope}[tnBlue]
				\fill (dotA) circle (2pt);
				\fill (dotB) circle (2pt);
			\end{scope}
		},
	},
	#1,
}

% ----------------------------------------
% Methodology (Teal - process/method)
% ----------------------------------------
\newtcolorbox{methodology}[1][]{%
	enhanced jigsaw,
	colback=tnBgHL, coltext=tnFg,
	colframe=tnTeal,
	size=small, boxrule=1pt,
	title=\textbf{\textcolor{tnTeal}{Methodology}},
	halign title=flush center,
	coltitle=tnTeal,
	breakable,
	drop shadow=tnBgDark!80!tnComment,
	attach boxed title to top left={xshift=1cm,yshift=-\tcboxedtitleheight/2,
		yshifttext=-\tcboxedtitleheight/2},
	minipage boxed title=1.5cm,
	boxed title style={%
		colback=tnBgDark, coltext=tnTeal,
		size=fbox, boxrule=1pt, boxsep=2pt,
		underlay={%
			\coordinate (dotA) at ($(interior.west) + (-0.5pt,0)$);
			\coordinate (dotB) at ($(interior.east) + (0.5pt,0)$);
			\begin{scope}
				\clip (interior.north west) rectangle ([xshift=3ex]interior.east);
				\filldraw [tnBgDark, blur shadow={shadow opacity=60, shadow yshift=-.75ex},
				rounded corners=2pt] (interior.north west) rectangle (interior.south east);
			\end{scope}
			\begin{scope}[tnTeal]
				\fill (dotA) circle (2pt);
				\fill (dotB) circle (2pt);
			\end{scope}
		},
	},
	#1,
}

% ----------------------------------------
% Contributions (Green - positive/achievements)
% ----------------------------------------
\newtcolorbox{contributions}[1][]{%
	enhanced jigsaw,
	colback=tnBgHL, coltext=tnFg,
	colframe=tnGreen,
	size=small, boxrule=1pt,
	title=\textbf{\textcolor{tnGreen}{Contributions}},
	halign title=flush center,
	coltitle=tnGreen,
	breakable,
	drop shadow=tnBgDark!80!tnComment,
	attach boxed title to top left={xshift=1cm,yshift=-\tcboxedtitleheight/2,
		yshifttext=-\tcboxedtitleheight/2},
	minipage boxed title=1.5cm,
	boxed title style={%
		colback=tnBgDark, coltext=tnGreen,
		size=fbox, boxrule=1pt, boxsep=2pt,
		underlay={%
			\coordinate (dotA) at ($(interior.west) + (-0.5pt,0)$);
			\coordinate (dotB) at ($(interior.east) + (0.5pt,0)$);
			\begin{scope}
				\clip (interior.north west) rectangle ([xshift=3ex]interior.east);
				\filldraw [tnBgDark, blur shadow={shadow opacity=60, shadow yshift=-.75ex},
				rounded corners=2pt] (interior.north west) rectangle (interior.south east);
			\end{scope}
			\begin{scope}[tnGreen]
				\fill (dotA) circle (2pt);
				\fill (dotB) circle (2pt);
			\end{scope}
		},
	},
	#1,
}

% ----------------------------------------
% Limitations / Criticism (Orange - caution/issues)
% ----------------------------------------
\newtcolorbox{limitations}[1][]{%
	enhanced jigsaw,
	colback=tnBgHL, coltext=tnFg,
	colframe=tnOrange,
	size=small, boxrule=1pt,
	title=\textbf{\textcolor{tnOrange}{Limitations / Criticism}},
	halign title=flush center,
	coltitle=tnOrange,
	breakable,
	drop shadow=tnBgDark!80!tnComment,
	attach boxed title to top left={xshift=1cm,yshift=-\tcboxedtitleheight/2,
		yshifttext=-\tcboxedtitleheight/2},
	minipage boxed title=1.5cm,
	boxed title style={%
		colback=tnBgDark, coltext=tnOrange,
		size=fbox, boxrule=1pt, boxsep=2pt,
		underlay={%
			\coordinate (dotA) at ($(interior.west) + (-0.5pt,0)$);
			\coordinate (dotB) at ($(interior.east) + (0.5pt,0)$);
			\begin{scope}
				\clip (interior.north west) rectangle ([xshift=3ex]interior.east);
				\filldraw [tnBgDark, blur shadow={shadow opacity=60, shadow yshift=-.75ex},
				rounded corners=2pt] (interior.north west) rectangle (interior.south east);
			\end{scope}
			\begin{scope}[tnOrange]
				\fill (dotA) circle (2pt);
				\fill (dotB) circle (2pt);
			\end{scope}
		},
	},
	#1,
}

% ----------------------------------------
% Discussion Points (Purple - thought-provoking)
% ----------------------------------------
\newtcolorbox{discussion}[1][]{%
	enhanced jigsaw,
	colback=tnBgHL, coltext=tnFg,
	colframe=tnPurple,
	size=small, boxrule=1pt,
	title=\textbf{\textcolor{tnPurple}{Discussion Points}},
	halign title=flush center,
	coltitle=tnPurple,
	breakable,
	drop shadow=tnBgDark!80!tnComment,
	attach boxed title to top left={xshift=1cm,yshift=-\tcboxedtitleheight/2,
		yshifttext=-\tcboxedtitleheight/2},
	minipage boxed title=1.5cm,
	boxed title style={%
		colback=tnBgDark, coltext=tnPurple,
		size=fbox, boxrule=1pt, boxsep=2pt,
		underlay={%
			\coordinate (dotA) at ($(interior.west) + (-0.5pt,0)$);
			\coordinate (dotB) at ($(interior.east) + (0.5pt,0)$);
			\begin{scope}
				\clip (interior.north west) rectangle ([xshift=3ex]interior.east);
				\filldraw [tnBgDark, blur shadow={shadow opacity=60, shadow yshift=-.75ex},
				rounded corners=2pt] (interior.north west) rectangle (interior.south east);
			\end{scope}
			\begin{scope}[tnPurple]
				\fill (dotA) circle (2pt);
				\fill (dotB) circle (2pt);
			\end{scope}
		},
	},
	#1,
}
